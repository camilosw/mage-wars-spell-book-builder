import React from 'react';

const SpellBook = () => {
  return (
    <div>
      <select name="mage" id="mage" />
      <select name="slot" id="slot" />
      <button>Reset</button>
    </div>
  );
};

export default SpellBook;
